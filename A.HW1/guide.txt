0) Њожете скачать и установить компилЯтор отдельно. проверьте, что компилЯтор доступен из командной строки.
„лЯ этого можно прописать gcc --version. если поЯвитсЯ ошибка типа "gcc не ЯвлетсЯ командной...", то вам необходимо
прописать путь до компилЯтора. ќтот компьютер -> свойства -> дополнительные параметры системы -> дополнительно -> 
параметры среды -> переменные среды пользователЯ -> Path -> создать. пишем адрес папки bin, котораЯ находитсЯ в папке
MinGW(у менЯ адрес выглЯдит так: C:\MinGW\bin). профит!
1) скачайте CLion с официального сайта
2) сборка и запуск проекта(зеленый молоточек и треугольник)
3) Ќаучитесь использовать файлы, которые не содержат код, но которые принадлежат проекту
(например, читать какие-то настройки). длЯ примера можете открыть файл CMakeList.txt, который создаетсЯ 
в CLion вместе с созданием cpp файлом
4) ѓорЯчие клавиши(как в IDEA) (https://devcolibri.com/горячие-клавиши-intellij-idea/)
   —астые вопросы:
   1) продублировать строку
      ctrl + D
   2) удалить строку
      ctrl + Y
   3) закоментить(раскоментить) // строку(несколько строк)
      ctrl + /
   4) закоментить(раскоментить) /* */ строку(несколько строк)
      ctrl + shift + /
   5) заменить название переменной во всем файле
      shift + f6
   6) просмотр реализации метода
      ctrl + леваЯ кнопка мыши
   7) просмотреть варианты решениЯ проблемки
      alt + enter
   8) собрать проект
      ctrl + f9
   9) запустить проект
      shift + f10
5) передача аргументов через командную строку (пример длЯ CLion)
   собираете проект. открываете Edit Configurations -> Application -> имЯ файла ->
   Program arguments: -> прописываете то, что хотите передать -> то, 
   что вы передадите передастсЯ в char *argv[](принимаетсЯ MAINом)
   вывести можно например так: cout << argv[1]; .
6) задефайнить значениЯ через командную строку
   рассмотрено в дз2
7) добейтесь того, чтобы ваша программа собиралась согласно C++17
   способы доказательства:
   1) сказать, что при создании указали C++17(плохой вариант)
   2) прописать в терминале gcc --version(плохой вариант)
   3) заюзать метод, который поддерживаетсЯ только в C++17(вариант получше)
   4) открыть файл CMakeList.txt. Указать на строку set(CMAKE_CXX_STANDARD 17)